/** Shared render options for transcript message components. */
export interface TranscriptRenderOpts {
  markdownRendering: boolean;
  syntaxTheme: string;
  backgroundZones: boolean;
  useUnicode: boolean;
}
